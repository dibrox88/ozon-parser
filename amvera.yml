# Конфигурация для Amvera.ru
# Используем Dockerfile для сборки
meta:
  environment: docker
  toolchain:
    name: docker

build:
  # Сборка через Dockerfile
  dockerfile: Dockerfile

run:
  # Команда запуска из Dockerfile будет использована автоматически
  containerPort: 8000
