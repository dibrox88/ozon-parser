# Руководство по синхронизации статусов разбитых товаров

## 📋 Обзор

Когда товар разбивается на несколько единиц (опция 3 "разбить на несколько штук"), все единицы должны иметь одинаковый статус в Google Таблице. Система автоматически синхронизирует статусы при обновлении данных.

---

## 🎯 Зачем это нужно?

**Проблема:** Если товар разбит на 3 единицы, и в Озоне статус одной единицы изменился на "получен", а других остался "забрать", это создаёт путаницу.

**Решение:** Система автоматически синхронизирует статусы всех единиц, выбирая приоритетный статус.

---

## 🔄 Как работает синхронизация

### Этап 1: Идентификация группы
Система группирует разбитые товары по следующим критериям:
- **Название товара** (`mapped_name`)
- **Номер заказа** (`order_number`)
- **Общее количество единиц** (`split_total`)

### Этап 2: Проверка статусов
Для каждой группы система проверяет, все ли единицы имеют одинаковый статус.

### Этап 3: Выбор приоритетного статуса
Если статусы различаются, выбирается приоритетный:

```
Приоритет статусов (от высшего к низшему):
1. получен           ← самый важный
2. в пункте выдачи
3. отменён
4. забрать           ← наименьший приоритет
```

### Этап 4: Синхронизация
Все единицы в группе получают приоритетный статус.

---

## 📊 Примеры

### Пример 1: Простая синхронизация

**До синхронизации:**
```
Товар: "Блок питания 500W"
Заказ: #12345
Разбит на: 3 единицы

Единица 1/3: забрать
Единица 2/3: получен    ← приоритетный!
Единица 3/3: забрать
```

**После синхронизации:**
```
Единица 1/3: получен
Единица 2/3: получен
Единица 3/3: получен
```

### Пример 2: Несколько групп товаров

**До синхронизации:**
```
Группа A: "Материнская плата" (2 единицы)
  1/2: забрать
  2/2: получен

Группа B: "Видеокарта" (3 единицы)
  1/3: в пункте выдачи
  2/3: забрать
  3/3: отменён
```

**После синхронизации:**
```
Группа A: "Материнская плата"
  1/2: получен
  2/2: получен

Группа B: "Видеокарта"
  1/3: в пункте выдачи
  2/3: в пункте выдачи
  3/3: в пункте выдачи
```

---

## 🔧 Техническая реализация

### Метаданные разбитых товаров

Каждая единица разбитого товара содержит следующие метаданные:

```python
{
    "is_split": True,           # Маркер разбитого товара
    "split_index": 2,           # Индекс текущей единицы (1, 2, 3...)
    "split_total": 5,           # Общее количество единиц
    "name": "Товар",            # Название товара
    "order_number": "#12345",   # Номер заказа
    "status": "получен"         # Текущий статус
}
```

### Столбцы в Google Sheets

Добавлены новые столбцы для хранения метаданных:

| Столбец | Описание | Пример |
|---------|----------|--------|
| J | `is_split` | True |
| K | `split_index` | 2 |
| L | `split_total` | 5 |

### Метод синхронизации

```python
def sync_split_items_status(self, order: Dict) -> None:
    """
    Синхронизировать статусы для разбитых товаров.
    Вызывается автоматически перед обновлением заказа.
    """
```

**Место вызова:** В методе `sync_orders()` перед проверкой изменений:

```python
for order in orders_list:
    # Синхронизируем статусы ПЕРЕД обновлением
    self.sync_split_items_status(order)
    
    # Проверяем изменения и обновляем
    if order_number in existing_orders:
        # ...обновление заказа
```

---

## 📝 Логи

Система выводит подробные логи при синхронизации:

```
⚠️ Разбитый товар 'Блок питания 500W' имеет разные статусы: 
   {'получен', 'забрать', 'в пункте выдачи'}

🔄 Синхронизация статуса на: 'получен'

✅ Заказ #12345 обновлён
```

---

## 🧪 Тестирование

Для тестирования синхронизации используйте:

```bash
python test_split_status_sync.py
```

**Тесты включают:**
1. ✅ Синхронизация трёх единиц с разными статусами
2. ✅ Проверка приоритетов статусов
3. ✅ Несколько групп разбитых товаров в одном заказе
4. ✅ Обычные товары не затрагиваются

---

## ⚙️ Настройки

### Изменение приоритетов

Если нужно изменить приоритеты статусов, отредактируйте метод `_get_priority_status()` в `sheets_sync.py`:

```python
def _get_priority_status(self, statuses: List[str]) -> str:
    status_priority = {
        'получен': 1,           # ← изменить приоритет
        'в пункте выдачи': 2,
        'отменён': 3,
        'забрать': 4
    }
    # ...
```

### Отключение уведомлений

Чтобы отключить уведомления в Telegram при синхронизации, закомментируйте:

```python
# sync_send_message(
#     f"🔄 <b>Синхронизация статусов</b>\n"
#     f"Товар: {group_data['name']}\n"
#     f"Единиц: {group_data['split_total']}\n"
#     f"Новый статус: {priority_status}"
# )
```

---

## 🔍 Устранение проблем

### Проблема: Статусы не синхронизируются

**Причина:** Метаданные `is_split`, `split_total` отсутствуют или неверны.

**Решение:**
1. Проверьте, что товар разбивался через опцию 3
2. Убедитесь, что все единицы имеют одинаковый `split_total`
3. Проверьте логи на наличие ошибок

### Проблема: Неверный приоритетный статус

**Причина:** Статус не входит в список приоритетов.

**Решение:**
1. Проверьте правильность написания статуса
2. Добавьте новый статус в метод `_get_priority_status()`
3. Укажите приоритет (число от 1 до 999)

---

## 📊 Визуализация в Google Sheets

### Формат отображения

```
| Дата       | Заказ  | Маркет | Статус | Формула | Цена | Название           | Тип    | is_split | index | total |
|------------|--------|--------|--------|---------|------|-------------------|--------|----------|-------|-------|
| 2025-11-03 | 12345  | Озон   | TRUE   | =SUM()  | 100  | Блок питания 500W | Расх.  | True     | 1     | 3     |
| 2025-11-03 | 12345  | Озон   | TRUE   | =SUM()  | 100  | Блок питания 500W | Расх.  | True     | 2     | 3     |
| 2025-11-03 | 12345  | Озон   | TRUE   | =SUM()  | 100  | Блок питания 500W | Расх.  | True     | 3     | 3     |
```

### Google Apps Script

Для визуального выделения разбитых товаров можно добавить в Apps Script:

```javascript
function highlightSplitItems() {
  var sheet = SpreadsheetApp.getActiveSheet();
  var data = sheet.getDataRange().getValues();
  
  for (var i = 1; i < data.length; i++) {
    if (data[i][9] === "True") {  // Столбец J: is_split
      var range = sheet.getRange(i + 1, 1, 1, 12);
      range.setBackground("#FFF3CD");  // Жёлтый фон
    }
  }
}
```

---

## 📚 См. также

- [SPLIT_UNITS_GUIDE.md](SPLIT_UNITS_GUIDE.md) - Руководство по разбивке товаров
- [SPLIT_REFACTORING.md](SPLIT_REFACTORING.md) - Детали рефакторинга
- [INTERFACE_UPDATE.md](INTERFACE_UPDATE.md) - Обновления интерфейса

---

**Дата:** 03.11.2025  
**Версия:** 1.0  
**Статус:** ✅ Протестировано
