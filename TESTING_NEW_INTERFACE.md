# 🧪 Тестирование нового интерфейса выбора товаров

## 📋 Чек-лист тестирования

### ✅ Сценарий 1: Товар не найден - вариант 1 (расходники)

**Шаги:**
1. Запустить `/parse`
2. Дождаться товара, которого нет в каталоге
3. Получить сообщение с вариантами 1-4
4. Отправить: `1`

**Ожидаемый результат:**
- Товар сохранен с типом "расходники"
- Имя товара = оригинальное название из заказа

---

### ✅ Сценарий 2: Товар не найден - вариант 2 (выбор из каталога)

**Шаги:**
1. Запустить `/parse`
2. Дождаться товара, которого нет в каталоге
3. Получить сообщение с вариантами 1-4
4. Отправить: `2`
5. Получить список типов (пронумерованный)
6. Отправить номер типа (например: `3`)
7. Получить список товаров этого типа
8. Отправить номер товара (например: `2`)

**Ожидаемый результат:**
- Товар сохранен с выбранным именем и типом из каталога
- Маппинг сохранен в `product_mappings.json`

**Проверить:**
```bash
cat product_mappings.json | grep "название_товара"
```

---

### ✅ Сценарий 3: Товар не найден - вариант 3 (разбивка)

**Шаги:**
1. Запустить `/parse`
2. Дождаться товара, которого нет в каталоге
3. Получить сообщение с вариантами 1-4
4. Отправить: `3`

**Ожидаемый результат:**
- Запускается процесс разбивки на компоненты
- Система запрашивает количество компонентов
- Для каждого компонента запрашивается название и тип

---

### ✅ Сценарий 4: Товар не найден - вариант 4 (исключение)

**Шаги:**
1. Запустить `/parse`
2. Дождаться товара, которого нет в каталоге
3. Получить сообщение с вариантами 1-4
4. Отправить: `4`

**Ожидаемый результат:**
- Весь заказ исключен из обработки
- Появляется сообщение: "🚫 Заказ XXXXXXXX исключён!"
- Заказ добавлен в `excluded_orders.json`

**Проверить:**
```bash
cat excluded_orders.json
```

---

### ✅ Сценарий 5: Найдены похожие товары - выбор по номеру

**Шаги:**
1. Запустить `/parse`
2. Дождаться товара с частичными совпадениями
3. Получить список из 5 вариантов
4. Отправить номер (например: `2`)

**Ожидаемый результат:**
- Товар сохранен с выбранным вариантом из списка
- Маппинг сохранен

---

### ✅ Сценарий 6: Найдены похожие товары - вариант 6 (выбор из каталога)

**Шаги:**
1. Запустить `/parse`
2. Дождаться товара с частичными совпадениями
3. Получить список из 5 вариантов + опции 6-8
4. Отправить: `6`
5. Выбрать тип
6. Выбрать товар

**Ожидаемый результат:**
- Товар сохранен с выбранным именем и типом из каталога

---

### ✅ Сценарий 7: Найдены похожие товары - вариант 7 (разбивка)

**Шаги:**
1. Запустить `/parse`
2. Дождаться товара с частичными совпадениями
3. Отправить: `7`

**Ожидаемый результат:**
- Запускается процесс разбивки

---

### ✅ Сценарий 8: Найдены похожие товары - вариант 8 (исключение)

**Шаги:**
1. Запустить `/parse`
2. Дождаться товара с частичными совпадениями
3. Отправить: `8`

**Ожидаемый результат:**
- Заказ исключен

---

## 🔍 Проверка типов в каталоге

**Команда для проверки типов:**
```python
from sheets_manager import SheetsManager
from product_matcher import ProductMatcher

sheets = SheetsManager()
products = sheets.get_all_products()
matcher = ProductMatcher(products)

print(matcher.get_type_list_message())
```

**Ожидаемый вывод:**
```
📋 Доступные типы товаров:

  1 - аксессуары
  2 - антивозрастная косметика
  3 - для волос
  ...
```

---

## 🐛 Возможные проблемы

### Проблема 1: "Некорректный выбор типа"

**Причина:** Введен нечисловой символ или номер вне диапазона

**Решение:** Система автоматически использует тип по умолчанию ("расходники")

---

### Проблема 2: "Нет товаров с типом X"

**Причина:** В Google Таблице нет товаров этого типа

**Решение:** 
1. Проверить Google Таблицу
2. Добавить товары нужного типа
3. Перезапустить парсинг

---

### Проблема 3: Пустой список типов

**Причина:** Каталог не загрузился из Google Таблицы

**Решение:**
1. Проверить подключение к Google Sheets
2. Проверить файл `google_credentials.json`
3. Перезапустить бота

---

## ✅ Критерии успеха

- [ ] Все 8 сценариев протестированы
- [ ] Числовые команды работают корректно
- [ ] Двухэтапный выбор (тип → товар) функционирует
- [ ] Последние товары из таблицы отображаются первыми
- [ ] Маппинги сохраняются в JSON
- [ ] Исключения заказов работают
- [ ] Разбивка на компоненты работает

---

## 📝 Лог тестирования

**Дата:** ________

**Тестировщик:** ________

| Сценарий | Статус | Примечания |
|----------|---------|------------|
| 1. Расходники | ⬜ | |
| 2. Выбор из каталога | ⬜ | |
| 3. Разбивка | ⬜ | |
| 4. Исключение | ⬜ | |
| 5. Выбор по номеру | ⬜ | |
| 6. Каталог (похожие) | ⬜ | |
| 7. Разбивка (похожие) | ⬜ | |
| 8. Исключение (похожие) | ⬜ | |

---

## 🚀 Запуск тестирования

### На локальной машине:

```bash
cd "C:\Users\фф\Documents\Apps\Ozon_Parser"
python main.py
```

### На сервере:

```bash
ssh ozon@85.193.81.13
cd ~/ozon_parser
source venv/bin/activate
python main.py
```

---

## 📊 Ожидаемые метрики

После тестирования проверить:

1. **Маппинги:** Количество новых записей в `product_mappings.json`
2. **Исключения:** Количество записей в `excluded_orders.json`
3. **Связки:** Количество записей в `product_bundles.json`
4. **Логи:** Отсутствие ошибок в консоли

---

## ✅ Готово к продакшену

Если все 8 сценариев прошли успешно - можно деплоить на сервер.

### Деплой:

```bash
# 1. Коммит изменений
git add product_matcher.py
git commit -m "feat: числовой интерфейс выбора товаров с двухэтапным выбором из каталога"

# 2. Пуш на сервер
git push origin main

# 3. На сервере
ssh ozon@85.193.81.13
cd ~/ozon_parser
git pull origin main

# 4. Перезапуск бота
sudo systemctl restart ozon-parser-bot
```
