# 🔄 Обновление интерфейса выбора товаров

## ✅ Изменения

### Замена буквенных команд на числовые

**Было:**
- `Р` - использовать тип "расходники"
- `В` - ввести вручную (формат: Название | Тип)
- `К` - разбить товар на компоненты
- `И` - исключить весь заказ
- `EXCLUDE` - исключить заказ (при совпадениях)

**Стало:**
- `1` - использовать тип "расходники"
- `2` - выбрать из каталога (сначала тип, затем товар)
- `3` - разбить на несколько штук (цена делится между единицами)
- `4` - исключить весь заказ

---

## 📋 Новая логика варианта 2 (выбор из каталога)

При выборе варианта `2` система запускает двухэтапный процесс:

### Этап 1: Выбор типа товара

Система показывает список всех типов из Google Таблицы:

```
📋 Доступные типы товаров:

  1 - аксессуары
  2 - антивозрастная косметика
  3 - для волос
  4 - для лица
  5 - парфюмерия
  6 - расходники
  ...

⏳ Введите номер типа:
```

**Пользователь отправляет:** номер типа (например, `3`)

### Этап 2: Выбор конкретного товара

После выбора типа система показывает товары этого типа:

```
📦 Товары типа 'для волос':

1. Шампунь восстанавливающий - 1200 ₽
2. Кондиционер увлажняющий - 950 ₽
3. Маска питательная - 1500 ₽
4. Сыворотка для кончиков - 800 ₽
...

⏳ Введите номер товара (1-10):
```

**Пользователь отправляет:** номер товара (например, `2`)

**Результат:** Система сохраняет выбранное название и тип

---

## 📊 Сравнение режимов

### Сценарий 1: Товар НЕ найден в каталоге

```
🔍 Товар НЕ НАЙДЕН в каталоге

📦 Товар из заказа:
• Название: Крем ночной
• Цвет: не указан
• Количество: 1
• Цена: 2500 ₽
• Заказ: 46206571-0697

❓ Предлагаем тип по умолчанию: расходники

💡 Варианты ответа:
1. Отправьте 1 - использовать тип "расходники"
2. Отправьте 2 - выбрать из каталога (сначала тип, затем товар)
3. Отправьте 3 - разбить на несколько штук
4. Отправьте 4 - исключить весь заказ 46206571-0697

⏳ Ожидаю ваш ответ...
```

### Сценарий 2: Найдены похожие товары

```
🔍 Найдены похожие товары

📦 Товар из заказа:
• Название: Сыворотка для лица
• Цвет: не указан
• Количество: 1
• Цена: 1800 ₽
• Заказ: 46206571-0698

✅ Предлагаемые варианты:
1. Сыворотка омолаживающая (антивозрастная косметика) - 95%
2. Сыворотка увлажняющая (для лица) - 92%
3. Сыворотка витаминная (для лица) - 90%
4. Сыворотка подтягивающая (антивозрастная косметика) - 88%
5. Крем-сыворотка (для лица) - 85%

💡 Варианты ответа:
• 1-5 - выбрать вариант по номеру
• 6 - выбрать из каталога (сначала тип, затем товар)
• 7 - разбить на несколько штук
• 8 - исключить весь заказ 46206571-0698

⏳ Ожидаю ваш ответ...
```

---

## 🔄 Алгоритм работы варианта 2

```python
# 1. Пользователь выбирает вариант 2
response = "2"

# 2. Система показывает типы из каталога (пронумерованные)
types = ["аксессуары", "для волос", "для лица", ...]

# 3. Пользователь выбирает тип (например, 3 = "для лица")
type_number = 3
selected_type = "для лица"

# 4. Система фильтрует товары по типу
products = [товары с типом "для лица"]

# 5. Сортировка: последние добавленные идут первыми
products = list(reversed(products))  # Реверс = последние из таблицы

# 6. Показываем до 10 товаров
products_to_show = products[:10]

# 7. Пользователь выбирает товар по номеру
product_number = 2
selected_product = products_to_show[1]  # индекс 1

# 8. Сохраняем выбранное название и тип
mapped_name = selected_product['name']
mapped_type = selected_product['type']
```

---

## 🎯 Преимущества нового интерфейса

1. **Числовой ввод удобнее** - не нужно переключать раскладку
2. **Двухэтапный выбор** - сначала категория, затем товар (логичнее)
3. **Последние товары первыми** - удобно если недавно добавили в таблицу
4. **Упрощенный выбор** - больше не нужен формат "Название | Тип"
5. **Прозрачность** - видно все варианты из каталога

---

## 📝 Примеры использования

### Пример 1: Быстрый выбор "расходники"

```
Товар: Салфетки влажные
Ответ: 1
Результат: Название="Салфетки влажные", Тип="расходники"
```

### Пример 2: Выбор из каталога

```
Товар: Крем для рук
Ответ: 2
→ Выбираем тип: 4 (для рук)
→ Выбираем товар: 1 (Крем питательный для рук)
Результат: Название="Крем питательный для рук", Тип="для рук"
```

### Пример 3: Разбивка на несколько штук

```
Товар: Набор (3 шт) - общая цена 900₽
Ответ: 3
→ Введите количество: 3
→ Система создаст 3 строки по 300₽ каждая
Результат: 3 отдельные строки в Google Таблице
```

### Пример 4: Исключение заказа

```
Товар: Ошибочный товар
Ответ: 4
Результат: Весь заказ исключен из обработки
```

---

## 🔧 Технические детали

### Файл: `product_matcher.py`

**Изменены функции:**
- `get_user_mapping_interactive()` - основная функция выбора

**Новые методы:**
- `get_type_list_message()` - формирует список типов
- `get_type_name(type_number)` - получает название типа по номеру

**Структура данных:**
```python
# Маппинг типов
type_map = {
    1: "аксессуары",
    2: "для волос",
    3: "для лица",
    ...
}

# Каталог товаров
catalog = [
    {'name': 'Шампунь', 'type': 'для волос', 'price': 1200},
    {'name': 'Крем', 'type': 'для лица', 'price': 1500},
    ...
]
```

---

## ✅ Готово к использованию

Все изменения внесены и протестированы. Система готова к работе с новым интерфейсом.

При следующем запуске `/parse` пользователи увидят обновленные варианты ответа с числовыми командами.
