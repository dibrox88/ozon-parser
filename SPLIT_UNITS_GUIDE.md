# 📦 Руководство по разбивке товаров на единицы

## 🎯 Назначение

Функция разбивки позволяет разделить один товар на несколько одинаковых единиц с автоматическим делением цены.

---

## 💡 Когда использовать?

### Подходящие случаи:
- ✅ **Наборы одинаковых товаров** (например, "Набор из 3 кремов")
- ✅ **Упаковки с несколькими единицами** (например, "5 помад в упаковке")
- ✅ **Комплекты** где нужно разделить цену поровну

### НЕ подходит:
- ❌ Товары с разными компонентами (используйте обычный маппинг)
- ❌ Товары где цена не делится поровну
- ❌ Единичные товары

---

## 🔄 Как работает?

### Шаг 1: Выбор опции разбивки

Когда товар не найден или найдены похожие варианты, выберите вариант **3** (или **7**):

```
💡 Варианты ответа:
1. Отправьте 1 - использовать тип "расходники"
2. Отправьте 2 - выбрать из каталога
3. Отправьте 3 - разбить на несколько штук  ← ВЫБИРАЕМ ЭТО
4. Отправьте 4 - исключить заказ
```

### Шаг 2: Указание количества единиц

Система запросит количество штук:

```
📦 РАЗБИВКА ТОВАРА НА НЕСКОЛЬКО ШТУК

Товар: Набор кремов (3 шт)
Цвет: не указан
Цена: 900 ₽
Количество: 1

💡 На сколько штук разбить?
⏳ Введите число штук (например: 2, 3, 5)...
```

**Отправьте число:** `3`

### Шаг 3: Автоматическое деление

Система автоматически:
1. Разделит цену: `900₽ / 3 = 300₽`
2. Создаст 3 одинаковых товара
3. Скорректирует последнюю цену для точности суммы

### Шаг 4: Подтверждение

```
✅ Товар разбит на 3 штук!

Товар: Набор кремов (3 шт)
Общая цена: 900 ₽
Цена за единицу: 300 ₽
Последняя единица: 300 ₽

Будет добавлено 3 строк в Google Таблицу.
```

---

## 📊 Результат в Google Таблице

### До разбивки:
| Дата | Заказ | Товар | Цена | Статус |
|------|-------|-------|------|--------|
| 01.11 | 12345-0001 | Набор кремов (3 шт) | 900₽ | Новый |

### После разбивки:
| Дата | Заказ | Товар | Цена | Статус |
|------|-------|-------|------|--------|
| 01.11 | 12345-0001 | Набор кремов (3 шт) | 300₽ | Новый |
| 01.11 | 12345-0001 | Набор кремов (3 шт) | 300₽ | Новый |
| 01.11 | 12345-0001 | Набор кремов (3 шт) | 300₽ | Новый |

---

## 🔧 Технические детали

### Структура разбитого товара

Каждая единица содержит:

```python
{
    'name': 'Набор кремов (3 шт)',  # Оригинальное название
    'color': '',
    'quantity': 1,  # Всегда 1 для каждой единицы
    'price': 300.0,  # Делённая цена
    'order_number': '12345-0001',
    'original_price': 900.0,  # Оригинальная цена
    'split_index': 1,  # Номер единицы (1, 2, 3...)
    'split_total': 3,  # Общее количество единиц
    'is_split': True,  # Маркер разбитого товара
    'mapped_name': 'Набор кремов (3 шт)',
    'mapped_type': 'расходники'
}
```

### Алгоритм деления цены

```python
# Цена за единицу (округляется до 2 знаков)
unit_price = round(900 / 3, 2)  # = 300.00

# Остаток для последней единицы
remainder = 900 - (300.00 * 2)  # = 300.00

# Если есть погрешность округления:
# Например: 1000 / 3 = 333.33
# Единицы: 333.33, 333.33, 333.34 (остаток корректируется)
```

### Ограничения

- **Минимум:** 2 единицы
- **Максимум:** 20 единиц
- **Автоматическая корректировка** последней цены для точности суммы

---

## 🔄 Обновление статусов

### Важно! Синхронизация статусов

Когда вы обновляете статус в Google Таблице для одной из разбитых единиц:

**❌ ПРОБЛЕМА:** Статус меняется только у одной строки
**✅ РЕШЕНИЕ:** Система автоматически обновит ВСЕ единицы

### Как работает?

1. Разбитые товары имеют `split_index` и `split_total`
2. При обновлении статуса система находит все связанные единицы
3. Статус меняется одновременно у всех единиц

### Пример синхронизации:

**Шаг 1:** Меняем статус первой единицы

| Товар | Цена | Статус | split_index | split_total |
|-------|------|--------|-------------|-------------|
| Набор (3 шт) | 300₽ | **Отправлен** | 1 | 3 |
| Набор (3 шт) | 300₽ | Новый | 2 | 3 |
| Набор (3 шт) | 300₽ | Новый | 3 | 3 |

**Шаг 2:** Система автоматически обновляет остальные

| Товар | Цена | Статус | split_index | split_total |
|-------|------|--------|-------------|-------------|
| Набор (3 шт) | 300₽ | **Отправлен** | 1 | 3 |
| Набор (3 шт) | 300₽ | **Отправлен** | 2 | 3 |
| Набор (3 шт) | 300₽ | **Отправлен** | 3 | 3 |

---

## 📋 Примеры использования

### Пример 1: Набор из 5 единиц

```
Товар: Помады (5 шт в наборе)
Цена: 2500₽
Выбор: 3 → 5

Результат:
- 5 строк в таблице
- Каждая по 500₽
- Сумма: 2500₽ ✅
```

### Пример 2: Деление с остатком

```
Товар: Кремы (3 шт)
Цена: 1000₽
Выбор: 3 → 3

Результат:
- Единица 1: 333.33₽
- Единица 2: 333.33₽
- Единица 3: 333.34₽ (скорректирована)
- Сумма: 1000.00₽ ✅
```

### Пример 3: Отмена разбивки

```
Товар: Набор
Цена: 600₽
Выбор: 3 → CANCEL

Результат:
- Разбивка отменена
- Товар сохранен как обычный (1 строка, 600₽)
```

---

## ⚠️ Важные моменты

### 1. Точность суммы

Система гарантирует что сумма всех единиц = оригинальная цена:

```python
# Проверка после разбивки
total = sum(item['price'] for item in split_items)
assert abs(total - original_price) < 0.01  # Допуск 1 копейка
```

### 2. Маппинг названий

После разбивки каждая единица проходит стандартный маппинг:

```
Оригинальное название → Остается как есть
Тип по умолчанию → "расходники"
```

### 3. Количество в каждой единице

Каждая разбитая единица имеет `quantity = 1`, даже если оригинал был `quantity = 2`.

### 4. Идентификация разбитых товаров

Проверка в коде:

```python
if item.get('is_split'):
    # Это разбитый товар
    total_units = item['split_total']
    current_index = item['split_index']
```

---

## 🚀 Интеграция с Google Таблицей

### Формула для проверки

В Google Таблице можно добавить столбец проверки:

```
=IF(AND(E2="Набор (3 шт)", COUNTIF(E:E, E2)=3), "✅", "⚠️")
```

Где:
- `E2` - столбец с названием товара
- Проверяет что у товара ровно 3 одинаковых строки

### Суммирование цен

Чтобы увидеть полную цену разбитого товара:

```
=SUMIF(E:E, E2, F:F)
```

Где:
- `E:E` - столбец с названиями
- `F:F` - столбец с ценами

---

## ✅ Готово!

Теперь вы можете эффективно работать с наборами и упаковками товаров, автоматически разделяя их на отдельные единицы в Google Таблице.

При возникновении вопросов - обращайтесь к этому руководству или проверяйте логи бота.
