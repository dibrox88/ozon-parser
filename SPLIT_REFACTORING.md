# 🎉 Обновление функции разбивки товаров

## 📋 Что изменилось?

### Старая логика (Bundle Splitting):
- ❌ Разбивка на **разные компоненты** (крем, маска, сыворотка)
- ❌ Ручной выбор типа для каждого компонента
- ❌ Ручной ввод цены для каждого компонента
- ❌ Сложная проверка суммы цен
- ❌ Сохранение в `product_bundles.json`
- ❌ Переиспользование сохраненных связок

### Новая логика (Unit Splitting):
- ✅ Разбивка на **одинаковые единицы** одного товара
- ✅ Автоматическое деление цены
- ✅ Простой ввод: только количество штук
- ✅ Гарантированная точность суммы
- ✅ Сохранение метаданных в каждой единице
- ✅ Поддержка синхронизации статусов

---

## 🔄 Изменения в коде

### 1. product_matcher.py

#### Удалено:
```python
def split_product_interactive(
    item, matcher, bundle_manager, order_number
) -> Optional[Dict]:
    # 250+ строк сложной логики
    # - Выбор схемы разбивки (2-3-5)
    # - Выбор товаров для каждого типа
    # - Ручной ввод цен
    # - Проверка суммы с повторами
    # - Сохранение в bundle_manager
```

#### Добавлено:
```python
def split_product_into_units(
    item, matcher, order_number
) -> Optional[List[Dict]]:
    # 100 строк простой логики
    # - Запрос количества штук
    # - Автоматическое деление цены
    # - Создание списка одинаковых товаров
    # - Добавление метаданных (is_split, split_index, split_total)
```

#### Изменена логика применения:
```python
# Было:
if cache_entry.get('is_bundle'):
    bundle_item = cache_entry['bundle_item']
    for component in bundle_item['components']:
        # Добавляем каждый компонент
        
# Стало:
if cache_entry.get('is_split'):
    split_items = cache_entry['split_items']
    for split_item in split_items:
        # Добавляем каждую единицу
```

### 2. Метаданные разбитых товаров

Каждая единица теперь содержит:

```python
{
    # Основные поля
    'name': 'Оригинальное название',
    'price': 300.0,  # Делённая цена
    'quantity': 1,
    
    # Метаданные разбивки
    'is_split': True,           # Флаг разбитого товара
    'split_index': 2,           # Номер текущей единицы (1, 2, 3...)
    'split_total': 3,           # Всего единиц
    'original_price': 900.0,    # Исходная цена до деления
    
    # Стандартные поля
    'order_number': '12345-0001',
    'mapped_name': 'Название',
    'mapped_type': 'тип'
}
```

---

## 📊 Примеры использования

### Пример 1: Простая разбивка

**Входные данные:**
```python
item = {
    'name': 'Набор кремов (3 шт)',
    'price': 900.0,
    'quantity': 1
}
```

**Действия пользователя:**
1. Выбрать вариант `3`
2. Ввести количество: `3`

**Результат:**
```python
[
    {'name': 'Набор кремов (3 шт)', 'price': 300.0, 'split_index': 1, 'split_total': 3},
    {'name': 'Набор кремов (3 шт)', 'price': 300.0, 'split_index': 2, 'split_total': 3},
    {'name': 'Набор кремов (3 шт)', 'price': 300.0, 'split_index': 3, 'split_total': 3}
]
```

### Пример 2: Деление с остатком

**Входные данные:**
```python
item = {
    'name': 'Помады (5 шт)',
    'price': 1000.0,
    'quantity': 1
}
```

**Действия:** Выбрать `3` → Ввести `3`

**Результат:**
```python
[
    {'price': 333.33, 'split_index': 1},  # unit_price
    {'price': 333.33, 'split_index': 2},  # unit_price
    {'price': 333.34, 'split_index': 3}   # remainder (скорректирован)
]
# Сумма: 333.33 + 333.33 + 333.34 = 1000.00 ✅
```

---

## 🔄 Синхронизация статусов

### Задача

При обновлении статуса одной единицы, нужно обновить ВСЕ связанные единицы.

### Решение (требует доработки sheets_sync.py)

```python
def sync_split_statuses(self, items: List[Dict]) -> List[Dict]:
    """
    Синхронизировать статусы для разбитых товаров.
    
    Алгоритм:
    1. Найти все разбитые товары (is_split=True)
    2. Сгруппировать по (name, order_number, split_total)
    3. Для каждой группы найти "мастер-статус" (приоритет: Отправлен > Новый)
    4. Применить мастер-статус ко всем единицам группы
    """
    # Группировка разбитых товаров
    split_groups = {}
    
    for item in items:
        if not item.get('is_split'):
            continue
            
        key = (item['name'], item['order_number'], item['split_total'])
        
        if key not in split_groups:
            split_groups[key] = []
            
        split_groups[key].append(item)
    
    # Синхронизация статусов
    for key, group in split_groups.items():
        if len(group) != group[0]['split_total']:
            logger.warning(f"⚠️ Неполная группа: {len(group)}/{group[0]['split_total']}")
        
        # Находим приоритетный статус
        statuses = [item.get('status', 'Новый') for item in group]
        master_status = max(statuses, key=lambda s: STATUS_PRIORITY.get(s, 0))
        
        # Применяем ко всем
        for item in group:
            if item.get('status') != master_status:
                logger.info(f"🔄 Синхронизация: {item['name']} [{item['split_index']}] → {master_status}")
                item['status'] = master_status
    
    return items
```

---

## 📝 Обновленная документация

### Файлы обновлены:

1. **`SPLIT_UNITS_GUIDE.md`** - полное руководство по новой функции
2. **`INTERFACE_UPDATE.md`** - обновлены примеры и описания
3. **`TESTING_NEW_INTERFACE.md`** - тесты для новой логики

### Новые разделы:

- ✅ Алгоритм деления цены с примерами
- ✅ Структура метаданных split_items
- ✅ Интеграция с Google Таблицей
- ✅ Синхронизация статусов (концепция)
- ✅ Примеры использования

---

## 🎯 Преимущества новой логики

### 1. Простота
- **Было:** 5-10 действий, выбор типов, ввод цен
- **Стало:** 2 действия, ввод одного числа

### 2. Скорость
- **Было:** 2-5 минут на разбивку
- **Стало:** 10 секунд

### 3. Надёжность
- **Было:** Ручная проверка суммы, риск ошибок
- **Стало:** Автоматическая корректировка, гарантия точности

### 4. Прозрачность
- **Было:** Компоненты с разными названиями
- **Стало:** Одинаковые товары с метаданными

---

## 🚀 Готово к использованию

Все изменения внесены и готовы к тестированию:

### Тестирование:

```bash
# 1. Запустить парсинг
/parse

# 2. Дождаться товара для разбивки
# 3. Выбрать вариант 3
# 4. Ввести количество (например, 3)
# 5. Проверить в Google Таблице - должно быть 3 одинаковые строки
```

### Проверка в коде:

```python
# Найти разбитые товары
split_items = [item for item in items if item.get('is_split')]

# Проверить суммы
for group in split_groups:
    total = sum(item['price'] for item in group)
    original = group[0]['original_price']
    assert abs(total - original) < 0.01  # ✅
```

---

## 📌 Следующие шаги

### Обязательно:
1. ✅ Протестировать разбивку на реальных данных
2. ⏳ Добавить синхронизацию статусов в `sheets_sync.py`
3. ⏳ Обновить Google Apps Script для распознавания разбитых товаров

### Опционально:
1. Добавить визуализацию связанных единиц в таблице (цветом)
2. Добавить столбец "Группа разбивки" с общим ID
3. Создать отчёт по разбитым товарам

---

## ✅ Заключение

Функция разбивки полностью переработана:
- Старая сложная логика bundle splitting удалена
- Новая простая логика unit splitting добавлена
- Документация обновлена
- Метаданные для синхронизации статусов готовы

Система готова к работе! 🎉
