# 🖥️ Информация для развертывания нового проекта на сервере

## Сервер
- **IP:** 85.193.81.13
- **Пользователь:** ozon
- **Пароль:** OzonParser2025!
- **SSH:** ssh ozon@85.193.81.13
- **Домашняя директория:** /home/ozon/

## ОС и окружение
- **ОС:** Ubuntu Server (22.04/24.04)
- **Timezone:** Europe/Moscow (MSK, +0300)
- **Python:** 3.12.x (venv)
- **Node.js:** (уточнить при необходимости)
- **Nginx:** установлен
- **Firewall:** ufw активен
- **Открытые порты:** 22, 80, 443, 8000

## Текущие проекты
- **ozon_parser**
  - Путь: /home/ozon/ozon_parser/
  - Виртуальное окружение: venv
  - API сервер: порт 8000
  - Systemd сервис: ozon-parser-api.service
  - Логи: /home/ozon/ozon_parser/logs/

## Права и доступ
- **Пользователь:** ozon (sudo доступ)
- **Группа:** ozon

## Рекомендации для нового проекта
- Размещать в /home/ozon/[имя_проекта]/
- Использовать отдельный venv
- Логи складывать в /home/ozon/[имя_проекта]/logs/
- Для API/web использовать свободные порты: 8001, 8002, 3000, 5000 и др.
- Для автозапуска создать systemd сервис ([имя_проекта].service)
- Для веб-доступа настроить nginx (reverse proxy)
- Firewall: открыть нужный порт через sudo ufw allow [порт]/tcp

## Пример структуры нового проекта
```
/home/ozon/[новый_проект]/
├── venv/
├── logs/
├── .env
├── requirements.txt
├── main.py
├── start_[проект].sh
├── stop_[проект].sh
```

## Полезные команды
```bash
# Проверка ресурсов
htop
free -h
df -h

# Проверка портов
sudo ss -tlnp | grep LISTEN

# Управление сервисами
ssh ozon@85.193.81.13 "cd ~/ozon_parser && ./stop_telegram_bot.sh && ./start_telegram_bot.sh" (пример)
sudo systemctl start [имя_проекта]
sudo systemctl stop [имя_проекта]
sudo systemctl status [имя_проекта]
sudo systemctl restart [имя_проекта]

# Firewall
sudo ufw status
sudo ufw allow [порт]/tcp

# Логи
ls -la ~/ozon_parser/logs/
tail -50 ~/ozon_parser/logs/[лог_файл].log
```

## Для Copilot
Передайте этот файл в новый чат Copilot для быстрого и корректного развертывания любого проекта на этом сервере.
