# Команда остановки парсера в Telegram боте

## ⏹️ Команда /stop

Добавлена новая команда для принудительной остановки работающего парсера.

### Как использовать

1. **Запустите парсер** через `/parse`
2. **Если нужно остановить** (например, слишком долго работает или произошла ошибка), отправьте `/stop`

### Что происходит при остановке

1. **Корректное завершение (terminate)**:
   - Бот отправляет сигнал SIGTERM процессу
   - Ждет 5 секунд на корректное завершение
   - Браузер закрывается, данные сохраняются

2. **Принудительное завершение (kill)**:
   - Если процесс не завершился за 5 секунд
   - Отправляется сигнал SIGKILL
   - Процесс убивается мгновенно

### Статусы команды

#### ℹ️ Парсер не запущен
```
Нечего останавливать. Используйте /status для проверки состояния.
```

#### ⚠️ Процесс не найден
```
Не удалось найти процесс парсера.
Возможно, он был запущен извне бота.
```

#### ✅ Остановлен корректно
```
Парсер остановлен
Процесс завершен корректно.
```

#### ✅ Остановлен принудительно
```
Парсер остановлен принудительно
Процесс был завершен жестко (kill signal).
```

## Обновленное меню команд

```
/start  - 🏠 Главное меню
/parse  - 🚀 Запустить парсинг
/stop   - ⏹️ Остановить парсинг (НОВОЕ!)
/status - 📊 Статус парсера
/help   - ❓ Справка
```

## Важные замечания

⚠️ **Ограничения:**
- Команда `/stop` работает только для парсера, запущенного через `/parse`
- Если парсер запущен через cron или вручную из консоли - нужно останавливать его отдельно

💡 **Когда использовать:**
- Парсер "завис" и не завершается
- Обнаружена ошибка и нужно срочно остановить
- Тестирование и нужно быстро прервать процесс
- Парсер работает слишком долго (хотя есть автоматический таймаут 30 мин)

## Проверка работы

### Тест 1: Остановка работающего парсера
```bash
# В Telegram:
1. /parse         # Запустить парсер
2. /stop          # Остановить через 10-20 секунд
3. /status        # Проверить что парсер больше не работает
```

### Тест 2: Остановка когда парсер не запущен
```bash
# В Telegram:
1. /status        # Убедиться что парсер не работает
2. /stop          # Попытаться остановить
   # Должно появиться: "ℹ️ Парсер не запущен"
```

## Технические детали

### Реализация
```python
# Глобальная переменная для хранения процесса
current_parser_process = None

# При запуске парсера
current_parser_process = subprocess.Popen(['python', 'main.py'], ...)

# При остановке
current_parser_process.terminate()  # Корректное завершение
current_parser_process.wait(timeout=5)  # Ждем 5 секунд

# Если не завершился
current_parser_process.kill()  # Принудительно
```

### Обновленные файлы
- `telegram_bot.py` - добавлена функция `stop_command()`
- Меню команд обновлено через BotCommand API
- Логирование всех операций остановки

## Развертывание

Команда уже развернута на сервере:
```bash
✅ Telegram бот запущен! PID: 110622
✅ Меню команд установлено
✅ Доступные команды: /start, /help, /status, /parse, /stop
```

### Проверка на сервере
```bash
ssh ozon@85.193.81.13 "tail -f ~/ozon_parser/logs/telegram_bot.log"
```

Смотрите логи в реальном времени:
- Запуск парсера через бота
- Остановка через /stop
- Корректное/принудительное завершение
